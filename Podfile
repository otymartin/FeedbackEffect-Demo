# Uncomment the next line to define a global platform for your project
platform :ios, '10.0'

target 'FeedbackEffect Demo' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'FeedbackEffect'
  # Pods for FeedbackEffect Demo

  target 'FeedbackEffect DemoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'FeedbackEffect DemoUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
